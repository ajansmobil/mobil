services:
  - type: web
    name: admin-mobil
    runtime: static
    buildCommand: echo "CI build — hosting.yml"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
